@model ProyectoCarnets.Models.ProgramaEducativo

@{
    ViewData["Title"] = "Eliminar Programa";
}

<div class="container mt-4">
    <div class="card shadow border-danger">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0"><i class="fa fa-exclamation-triangle"></i> Eliminar Programa Educativo</h4>
        </div>
        <div class="card-body">
            <h5 class="card-title text-center text-danger mb-4">¿Estás seguro que deseas eliminar este registro?</h5>

            <div class="row justify-content-center">
                <div class="col-md-8">
                    <table class="table table-bordered">
                        <tbody>
                            <tr>
                                <th class="bg-light" style="width: 30%">Clave:</th>
                                <td>@Model.Clave</td>
                            </tr>
                            <tr>
                                <th class="bg-light">Nombre del Programa:</th>
                                <td>@Model.NombrePrograma</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="d-flex justify-content-center mt-3 gap-3">
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />

                    <button type="submit" class="btn btn-danger btn-lg px-4">
                        <i class="fa fa-trash"></i> Confirmar Eliminación
                    </button>
                </form>

                <a asp-controller="Carnets" asp-action="Index" class="btn btn-secondary btn-lg px-4 ms-2">
                    Cancelar
                </a>
            </div>

        </div>
    </div>
</div>